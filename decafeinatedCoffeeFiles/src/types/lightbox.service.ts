/**
 * Placeholder interface for refactoring .coffee files into modern JS framework.
 * Can be extended as needed.
 */
export interface LightboxService {
    getLightboxUrl(): string;
    getLightboxType(): string;
    getLightboxName(): string;
    getLightboxId(): number;
}
